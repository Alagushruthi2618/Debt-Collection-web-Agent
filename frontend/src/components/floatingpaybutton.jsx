import { useEffect, useState } from "react";

function FloatingPayButton({ outstandingAmount, daysPastDue, onPayNow, isVerified }) {
  // Always return null to remove the floating popup
  return null;
}

export default FloatingPayButton;

